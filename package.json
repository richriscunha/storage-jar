{
  "name": "storage-jar",
  "version": "1.0.6",
  "description": "A minimal API to handle cookies or local storage items.  Allows for the ability to write, read, delete, and contains.",
  "main": "dist/storage-jar.min.js",
  "scripts": {
    "build": "npm run clean && rollup -c && gulp compress",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cookies",
    "local storage",
    "localStorage",
    "cookie"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/richriscunha/storage-jar.git"
  },
  "author": "Richard Cunha",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015-rollup": "^1.2.0",
    "gulp": "3.9.1",
    "gulp-minify": "0.0.14",
    "gulp-rename": "1.2.2",
    "gulp-uglify": "2.0.0",
    "rimraf": "2.5.4",
    "rollup": "^0.34.13",
    "rollup-plugin-babel": "^2.6.1"
  }
}
